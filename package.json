{
    "name": "fin-edge",
    "version": "1.0.0",
    "description": "Group Assignment for backend engineering launchpad",
    "main": "index.js",
    "scripts": {
        "pretest": "node -e \"if (process.version.slice(1).split('.')[0] < 18) { console.error('Unsupported Node.js version, please use version 18'); process.exit(1); }\"",
        "test": "tap test/*.js --disable-coverage",
        "start": "nodemon app.js"
    },
    "author": "Airtribe",
    "license": "ISC",
    "devDependencies": {
        "nodemon": "^3.1.11",
        "supertest": "^6.3.4",
        "tap": "^18.6.1"
    },
    "dependencies": {
        "bcrypt": "^6.0.0",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^8.22.1"
    },
    "engines": {
        "node": ">= 18.0.0"
    }
}
